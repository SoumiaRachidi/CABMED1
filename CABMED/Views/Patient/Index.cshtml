@{
    ViewBag.Title = "Espace Patient";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container" style="margin-top:30px;">
    <div class="jumbotron">
        <h2>Bienvenue dans votre espace, @ViewBag.UserName</h2>
        <p class="lead">Ceci est le tableau de bord patient (placeholder).</p>
        <p>
            <a class="btn btn-primary btn-lg" href="@Url.Action("Logout", "Auth")" role="button">Se déconnecter</a>
        </p>
    </div>
</div>
